# 흥K병원 글로벌 홈페이지 개발 인수인계

작성일: 2026-09-18  
이 문서는 학원에서 진행한 퍼블리싱 작업을 고정 대화 **“흥케이병원 최종 퍼블리싱 구현”**에서 계속하기 위한 기준 문서다.

## 1. 프로젝트 기준

- 사이트: 흥K병원 글로벌 홈페이지 한국어 버전 기준
- Figma 메인페이지 1안: https://www.figma.com/design/4cetoACzx20LgtH4pVaVsc/%ED%9D%A5%EC%BC%80%EC%9D%B4%EB%B3%91%EC%9B%90-%EA%B8%80%EB%A1%9C%EB%B2%8C-%EC%82%AC%EC%9D%B4%ED%8A%B8-%ED%95%9C%EA%B8%80%EB%B2%84%EC%A0%84-%EA%B8%B0%EC%A4%80?node-id=30-731
- 개발 방식: HTML + CSS + Vanilla JavaScript
- 프레임워크: 사용하지 않음
- 개발 순서: 전체를 한꺼번에 만들지 않고 Figma의 섹션을 한 개씩 구현한다.
- 반응형: PC, 태블릿, 모바일을 매 섹션에서 함께 처리한다.
- 모바일 헤더는 사용자가 디자인을 다시 만든 뒤 수정한다.
- 소스 저장과 동기화는 사용자가 GitHub에서 관리한다. 새 환경에서는 먼저 최신 저장소와 작업 브랜치를 확인한다.

## 2. 가장 중요한 편집 원칙

1. 이미지, 아이콘, 카드, 메뉴, 마키 사진, 통계 숫자 등 눈에 보이는 콘텐츠 구조는 `index.html`에 직접 작성한다.
2. JavaScript로 콘텐츠 DOM을 생성하거나 복제하지 않는다.
3. JavaScript에서 이미지의 `src`를 넣거나 바꾸지 않는다.
4. JavaScript는 번역 문구 연결, 이벤트, 스크롤 감지, 애니메이션에만 사용한다.
5. 한국어 표시 문구의 원본은 HTML에 남겨 JS가 없어도 내용을 읽을 수 있게 한다.
6. 번역 대상 문구는 `locales/ko.js`의 키와 연결한다.
7. 숫자의 최종값은 HTML에 적고, JS는 그 값을 읽어 0부터 카운트업한다.
8. 이미지 교체는 HTML의 `<img src="...">` 한 곳에서 할 수 있어야 한다.
9. 기존 구조와 완성된 섹션을 불필요하게 갈아엎지 않는다.
10. 사용자가 수정하기 쉬운 코드와 한국어 주석을 유지한다.

## 3. 다국어 구조

- 예정 언어: 한국어 `ko`, 영어 `en`, 중국어 `zh`, 일본어 `ja`, 러시아어 `ru`, 몽골어 `mn`, 힌디어 `hi`, 아랍어 `ar`
- URL은 `/ko/`, `/en/` 형태를 사용한다.
- 현재 실제 번역 콘텐츠는 한국어만 연결되어 있고 나머지 언어 버튼은 비활성 상태다.
- 아랍어를 위해 `dir="rtl"` 구조를 유지한다.
- 언어와 번역 연결: `js/i18n.js`
- 한국어 번역 문구: `locales/ko.js`
- 메뉴와 이미지 목록은 JS가 만들지 않고 HTML에 직접 둔다.

## 4. 현재 파일 역할

- `index.html`: 모든 섹션의 실제 콘텐츠, 이미지 경로, 아이콘, 통계 숫자
- `css/header.css`: 공통 헤더
- `css/hero.css`: 첫 번째 히어로 배너
- `css/about.css`: About HEUNG-K 소개, 통계 카드, 사진 마키
- `css/global.css`: GLOBAL EXCELLENCE / HSS IN KOREA / 5년·17년 영역
- `js/header.js`: 초기화와 각 섹션 렌더 함수 연결, 언어 선택
- `js/hero.js`: 히어로 숫자·선·텍스트 애니메이션
- `js/about.js`: About 번역, 통계 카운트업, 마키 동작
- `js/global.js`: GLOBAL EXCELLENCE 번역, 등장 효과, 숫자 카운트업, 워터마크 스크롤 투명도
- `EDITING.md`: 콘텐츠 수정 위치를 사용자가 빠르게 찾는 안내서
- `server.cjs`: 로컬 미리보기 서버, `http://127.0.0.1:4173/ko/`

## 5. 구현 완료 상태

### 공통 헤더

- 데스크톱 기준 로고, 글로벌 언어 버튼, 임시 전화번호, 주 메뉴가 구현되어 있다.
- 모바일 헤더는 최종 디자인을 기다리는 상태다.

### 히어로 배너

- 연간 `7,208례` 숫자가 0부터 2.4초 동안 올라간다.
- 숫자 좌우 선이 중앙에서 바깥쪽으로 그려진다.
- 제목과 설명이 화면에 나타날 때 이동하며 보인다.
- 세로 숫자 롤링 방식은 사용자가 취소했으며 일반 숫자 카운트업을 유지한다.

### About HEUNG-K

- 섹션 제목과 설명이 아래에서 위로 나타난다.
- 통계: 병상 수 82, 외래환자 400, 입원환자 6,152, 국제환자 90, 수술 수 7,200.
- 숫자는 HTML 값을 읽어 0부터 2.4초 동안 카운트업한다.
- 위쪽 사진 줄은 왼쪽으로, 아래쪽 사진 줄은 오른쪽으로 천천히 이동한다.
- 마키 사진은 HTML에 한 번씩만 있으며 JS가 복제하지 않는다.
- 사진 움직임 멈춤/재생 버튼과 `prefers-reduced-motion` 처리가 있다.
- 수술 수의 청진기 아이콘은 현재 여러 SVG 조각으로 되어 있다. 사용자가 완성 SVG를 준비하면 `index.html`의 해당 `<img>` 묶음을 새 SVG 한 장으로 교체한다.

### GLOBAL EXCELLENCE / HSS IN KOREA / 5년·17년

- About 아래에 2번 섹션으로 추가되어 있다.
- 큰 `HSS IN KOREA` 문구는 뒤쪽에서 sticky로 따라온다.
- 워터마크 불투명도는 섹션 진입 시 100%에서 시작해 아래로 스크롤할수록 낮아지고 최소 15%가 된다. 위로 스크롤하면 다시 진해진다.
- 중앙 `HEUNG-K for Special Surgery`와 본문은 왼쪽에서 오른쪽으로 나타난다.
- 오른쪽 수술 사진은 blur 상태에서 서서히 선명해진다.
- Figma 원본 수술 사진을 가져오지 못해 현재 `/assets/about/background.png`를 임시 사용한다. 원본을 받으면 `index.html`의 `data-global-photo` 내부 `src`만 교체한다.
- 금색 화관 안의 5와 17은 일반 숫자 카운트업 방식이다.
- 데스크톱에서는 화관 2개를 나란히, 모바일에서는 세로로 배치한다.
- `HSS IN KOREA`의 sticky 구간을 늘리려면 이후 HSS 콘텐츠를 `global-stage` 내부에 이어서 추가한다.

### HSS 소개와 글로벌 의료 교류

- 구현 범위는 ‘세계 정형외과의 기준 HSS’와 ‘HSS에서 배우고, 흥K의 전문성을 공유하다’ 사진 콜라주까지다.
- 다음 ‘최소절개 인공관절 수술’ 영역은 아직 구현하지 않았다.
- 잘못 추가했던 FACILITIES 둘러보기는 제거했다. 프레임 이름만 보고 내용을 추측하지 말고 실제 문구와 위치를 확인한다.
- 사진 10장은 로컬 Figma 백업의 원본을 assets/hss/에 추출했다. HTML img에서 직접 수정한다.
- 한국어 제목·설명은 index.html의 hss-story가 원본이며 다른 언어는 data-global 키로 연결한다.
- css/hss.css에서 PC 비대칭 콜라주, 태블릿 2열, 모바일 세로 배열을 관리한다.
- 기존 global.js의 텍스트 등장 효과와 sticky 배경 100% → 15% 변화를 확장했다.

## 6. 애니메이션 공통 기준

- 화면 진입 감지: `IntersectionObserver`
- 숫자: 0 → HTML 최종값, 약 2.4초, cubic ease-out
- 기본 텍스트: 섹션 요구 방향으로 이동하면서 opacity 0 → 1
- GLOBAL 수술 사진: blur 20px + opacity 0 → blur 0 + opacity 1, 약 2.2초
- `HSS IN KOREA`: scroll 진행률에 따라 opacity 1 → 0.15
- 모션 감소 설정 사용자는 즉시 최종 상태를 보여준다.
- 콘텐츠를 가리거나 JS 오류 때 영구적으로 숨기지 않도록 기본 HTML은 보이는 상태여야 한다.

## 7. 디자인과 자산 관련

- 디자인 구현에는 Figma 편집 권한이 필요하지 않으며 보기 가능한 디자인 파일 링크면 충분하다.
- 동작 확인이 필요하면 프로토타입 링크를 함께 사용한다.
- 현재 Figma MCP Starter 사용량 제한에 도달한 적이 있다. 결제는 프로젝트 진행의 필수 조건이 아니다.
- MCP 제한 시 공개 Figma 프로토타입, 사용자가 제공한 이미지, 기존 로컬 자산을 기준으로 작업한다.
- 실제 이미지와 아이콘을 임의로 새로 그리지 않는다. 원본 자산을 받으면 HTML의 `src`를 교체한다.

## 8. 검증 기준

- 수정 후 관련 JS 파일에 문법 오류가 없는지 확인한다.
- 브라우저 콘솔 오류가 없는지 확인한다.
- PC, 태블릿, 모바일에서 가로 스크롤이 생기지 않는지 확인한다.
- 이미지가 모두 로드되는지 확인한다.
- 숫자가 목표값에 정확히 도달하는지 확인한다.
- 마키의 방향, 일시정지 버튼, 모션 감소 설정을 확인한다.
- 새 섹션을 추가할 때 기존 히어로와 About 애니메이션이 깨지지 않는지 확인한다.

## 9. 다음 작업을 시작하는 방법

1. GitHub에서 사용자가 지정한 최신 저장소와 브랜치를 연다.
2. 이 문서와 저장소의 `AGENTS.md`, `EDITING.md`를 먼저 읽는다.
3. 로컬 미리보기를 실행하고 `/ko/`에서 현재 상태를 확인한다.
4. Figma 메인페이지 1안에서 사용자가 지정한 다음 섹션만 구현한다.
5. 작업 후 무엇을 왜 수정했는지 한국어로 설명한다.

## 10. 현재 주의 사항

- 헤더의 전화번호는 시안용 임시값 `+82-10-1111-2222`다. 실제 번호 확인 후 변경해야 한다.
- GLOBAL 영역의 수술 사진은 임시 이미지다.
- 모바일 헤더는 사용자 새 디자인 대기 중이다.
- Figma의 전체 페이지 노드 `30:731`은 매우 커서 한 번에 읽기 어렵다. 다음부터는 구현할 섹션을 Figma에서 선택한 노드 링크로 받는 것이 좋다.
- GitHub 원격 주소와 사용할 브랜치는 이 문서에 추측해서 기록하지 않았다. 새 작업에서 사용자가 연결한 저장소 상태를 기준으로 한다.

## 11. 사용자 협업 방식

- 사용자는 디자인과 콘텐츠를 직접 자주 수정한다. 수정할 위치를 찾기 쉬워야 한다.
- 매 단계에서 변경한 내용과 이유를 설명한다.
- 한 섹션이 끝나면 화면을 확인하고 다음 섹션으로 넘어간다.
- 사용자가 제공하는 최신 디자인과 지침을 우선한다.
- 대화에서는 어렵지 않은 한국어로 친근하게 설명한다.

## 12. MIS TKA 퍼블리싱 (2026-09-22)
- 최소절개 인공관절 수술부터 ‘무엇이 다른가요?’ 비교 설명까지 index.html에 추가했다.
- CSS: css/mis.css / 애니메이션: js/mis.js / 사진: assets/mis/.
- 원본 이미지와 한국어 콘텐츠는 HTML에서 직접 수정한다. 번역은 locale의 mis 키로 연결한다.
- 의사 배경은 블러 해제, 문구는 왼쪽에서 등장한다. 중앙 무릎뼈가 먼저 1.1초 동안 나타나고 왼쪽 문구는 1.25초, 오른쪽 문구는 1.5초에 반대 방향에서 등장한다.
- 왼쪽 MIS TKA · Minimally Invasive TKA / 3 표시는 비교 설명 끝까지 sticky다.
- 중앙 이미지는 원본의 비대칭 여백 때문에 translateX(-71%)로 뼈 중심을 맞췄다.
- 미완료: 비교 설명 옆 아이콘 3개는 원본을 확보하지 못했다. 원본 SVG가 제공되면 mis-benefit-list의 각 li 안, 텍스트 div 앞에 img 태그로 추가한다. 이미지 크기 스타일은 준비되어 있다.
- 이후의 파란색 치료 소개 배너는 이번 구현 범위에 포함되지 않는다.

### 2026-09-22 헤더·MIS 보완
- MIS 아이콘 3개 원본 연결 완료: assets/mis/icon-mis1.svg ~ icon-mis3.svg. HTML의 mis-benefit-list에서 수정합니다. 이전 아이콘 대기 안내는 해결되었습니다.
- 의사 배경 불투명도는 css/mis.css의 .mis-doctor img에서 수정합니다 (기본 30%, 모바일 25%).
- 고정 헤더는 아래로 스크롤 시 2층 메뉴만, 위로 스크롤 시 1·2층 모두 표시합니다. css/header.css와 js/header.js 하단의 한국어 주석을 참고하세요.


## 차별화된 치료 / JK Minimal (2026-09-22)
- 구현 범위: ‘흥K병원은 차별화된 치료를 제공합니다’ → 특허 액자·JK Minimal → 최대 효율/최소 손상/최소 절개 → 전략 카드 3개까지.
- HTML에서 differentiated-treatment를 검색해 문구와 이미지 src를 수정합니다. 이미지 원본은 assets/treatment/입니다. Mobile Window Technique는 현재 프로토타입과 같이 텍스트 카드입니다.
- css/treatment.css에서 PC·모바일 배치와 애니메이션 시간(1.1초), 이동 거리(문구 36px, 액자 60px)를 수정합니다.
- js/treatment.js는 번역과 화면 진입 효과만 연결합니다. data-treatment-reveal="up"은 아래에서 위, 기본 속성은 왼쪽에서 오른쪽입니다. 각 요소는 한 번만 실행됩니다.
- locale의 treatment에 data-treatment 키와 같은 이름으로 번역을 추가할 수 있습니다. JS 비활성화나 모션 감소 설정에서도 본문은 표시됩니다.


### 2026-09-23 치료 영역 보완
- 사용자 수정 CSS(배경 높이, PC·모바일 리본 위치, 360px 이하 기구 크기)를 보존한 채 도면 배경과 연결선을 추가했습니다.
- 도면 배경은 index.html의 treatment-blueprint 이미지입니다. 원본: assets/treatment/heungk-white-mint-blueprint-bg.png.
- 기구 이미지 선택자는 treatment-instrument-image로 구분했습니다. 배경에 기구 회전·크기 스타일이 적용되지 않도록 하기 위함입니다.
- 연결선 태그는 HTML에 있으며, js/treatment.js의 connectTreatmentLabels가 창 크기·문구 크기 변경 시 좌표만 갱신합니다. 모바일 최대 효율 연결선은 설명 문단 바깥 여백을 따라갑니다.
- 연결선 색과 두께는 css/treatment.css 하단 treatment-connector 규칙에서 수정합니다.


## 작은 절개, 눈으로 확인되는 차이 (2026-09-23)
- 범위: 68세·72세·79세 비교 사진 3장 및 최소절개 인공관절 수술의 장점 4개까지.
- index.html에서 incision-results 검색: 이미지 src, 나이, 점선 위치(--mark-*), 문구를 직접 수정합니다.
- 이미지: assets/incision/case-68.png, case-72.png, case-79.png. Figma 백업의 원본 이미지를 사용합니다.
- 배치: css/incision.css. PC 사진 3열, 700px 이하 사진 1열. 장점은 PC 4열, 태블릿 2열, 작은 모바일 1열입니다.
- 효과: js/incision.js. 문구는 왼쪽에서, 사진은 아래에서 1.1초 동안 한 번 등장합니다. 모션 감소 설정에서는 즉시 표시됩니다.
- 다국어: locale의 incision 객체에 data-incision 키와 같은 번역 문구를 추가합니다. 한국어 원본은 HTML이며 이미지·나이는 번역 파일에 넣지 않습니다.
- 장점 아이콘: assets/treatment/tj1.svg ~ tj4.svg를 각 li의 h4 앞에 HTML img 태그로 연결했습니다. 크기와 간격은 css/incision.css의 .incision-benefit-icon에서 수정합니다.


## CADAVER 워크숍 → 트로피 (2026-09-23)
- 범위: 4 / CADAVER 소개와 파란 배경 사진, 금빛 액자 2개, 중앙 트로피까지. 다음 섹션은 추가하지 않았습니다.
- index.html에서 id="cadaver" 검색: 문구·이미지 경로를 직접 수정합니다. 번역은 locale의 cadaver 객체와 data-cadaver 키로 연결됩니다.
- css/cadaver.css: 파란 배경, 금빛 제목, 사진 배치, 반응형 및 등장 효과. 기존 섹션의 CSS는 변경하지 않았습니다.
- js/cadaver.js: 문구 번역 및 화면 진입 시 한 번 실행하는 효과. 문구는 왼쪽→오른쪽(1.1초), 배경은 블러 해제(2.2초), 트로피는 아래→위(1.4초). 모션 감소 설정과 JS 비활성화 시에도 표시됩니다.
- assets/cadaver/workshop-group-framed.svg, workshop-collage-framed.svg: 원본 액자와 사진을 하나로 조합한 완성 이미지. 사진을 웹용 WebP로 압축해 SVG 안에 내장하여 외부 파일 참조가 없습니다. 웹페이지에서는 액자당 img 하나만 사용합니다.
- 모바일은 액자 2개를 세로로 배치하고 트로피를 아래에 두어 사진을 가리지 않습니다.


## 연구 성과 / PUBLICATIONS (2026-09-23)
- 범위: `#research`의 “임상 경험을 연구 성과로 이어갑니다”부터 논문 액자까지.
- 문구, 원장 사진, 캡션, 액자는 `index.html`에 직접 작성했습니다. `assets/research/`에서 이미지를 교체할 수 있습니다.
- 사진은 `Frame 53` 시안의 AAOS 발표 / 연세대학교 워크숍 / 정형외과 학술 발표 3종입니다. 무한 슬라이드에 필요한 동일한 두 벌을 HTML에 작성했으므로 사진 교체 시 두 곳을 함께 수정합니다.
- Swiper 12.2.0은 `assets/vendor/swiper/`에 저장했습니다. 외부 CDN 접속 없이 작동하고 라이선스도 포함합니다. 참고: https://swiperjs.com/swiper-api
- 사진 자동 전환: `js/research.js`의 `autoplay.delay: 2000`(2초), `speed: 650`(0.65초 이동). 직접 넘기기·터치 스와이프·재생 정지 지원.
- 액자 마키: `css/research.css`의 `research-marquee` 80초(모바일 70초). 동일한 A/B 목록을 HTML에 작성해 CSS만으로 연결합니다. JS 이미지 생성/복제 없음.
- 액자는 실제 흰 액자와 논문 원본을 함께 포함한 SVG 한 장씩입니다. 한국어 문구가 HTML 원본이고 `data-research`에 맞는 locale.research 번역 문자열만 선택적으로 연결합니다.
- 글자 좌→우 등장은 1.1초. 화면 밖/백그라운드에서는 자동 움직임 정지. OS의 동작 줄이기에서는 자동 슬라이드와 마키를 끄고 수동 탐색을 유지합니다.
- 기존 사용자 CSS는 수정하지 않고 연구 성과 전용 파일만 추가했습니다.
